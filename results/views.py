from django.shortcuts import render


def index(request):
    """
    Have a search bar
        search key words
        return ranked results

    addition features
        display results while typing search
        pagination
    """
    pass
