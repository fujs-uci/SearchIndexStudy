import pandas as pd
import ast

# df = pd.read_csv("../loc/file.csv")
#   df.min(), df.max(), df.mean(), df.median()
# column = df[column].apply(lambda x: ast.literal_eval(x))
#   turns x into literal type (ie string of a list to list)
